# 产品验证

## 当前产品

一个不接管麦克风的 Pi 扩展：用户单独执行一次 `/voice`，便能在所有 Pi 会话中开启或关闭外部语音转写容错模式。

## 价值信号

- 用户开启后可以继续直接说话，不必反复解释 Typeless 的同音词、断句和自我纠正。
- 新会话与重启后仍保持同一状态。
- 状态栏只用 `🎙` 与 `🔇` 表达状态。

## 发布前验证

- 自动测试覆盖开关、持久化、跨会话同步、提示注入、误带参数和损坏状态文件。
- 真实 Pi 终端能发现 `/voice`，并在切换后显示正确图标。
- 发布包只包含扩展、说明与许可证，不包含音频采集、网络请求、遥测或本机状态文件。
